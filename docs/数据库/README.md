# 数据库

本目录描述框架的 `db` 包当前实现的数据库能力。每个文件只负责一个主题，示例遵循应用层的调用边界：

```text
Controller -> Validator -> Service -> Model/ORM -> Database
```

控制器只负责接收请求、调用验证器和服务，不直接调用模型或查询构造器。服务层优先使用模型和 ORM；只有 ORM 无法清晰表达的复杂查询才使用参数化原生 SQL。

## 应用层约定

- 所有业务模型直接放在 `app/<应用名>/model`，不在该应用的 `model` 目录下继续创建业务子目录。
- 一张真实表对应一个模型文件，文件使用真实表名命名，不增加 `Model`、`Entity` 等后缀；例如 `users.go`、`order_items.go`。
- 优先使用 `db.NewModelAuto` 自动推断表名。它只把具名结构体名转换为小写蛇形，不自动复数化；推断结果和真实表名不一致时，使用 `db.NewModel` 或 `Model.Table` 显式绑定。
- 模型查询和写入放在服务或仓储层，服务通过 `WithContext` 传递请求上下文。

## 连接与生命周期

- [连接数据库](连接数据库.md)：连接配置、连接器、连接池和支持的驱动。
- [连接管理](连接管理.md)：`DB`、`Manager`、默认连接、多连接和关闭生命周期。
- [方言与兼容性](方言与兼容性.md)：SQL 方言、占位符、分页、锁和非 SQL 连接差异。

## 查询构造

- [查询构造器](查询构造器.md)：`DB.Name`、`DB.Table`、链式查询、克隆和上下文。
- [查询条件](查询条件.md)：安全条件、条件组、时间、集合、原生条件入口。
- [查询字段与排序](查询字段与排序.md)：字段投影、别名、排序、分组、`HAVING` 和去重。
- [查询连接与锁](查询连接与锁.md)：JOIN、前缀解析、投影冲突和悲观锁。
- [聚合与字段值](聚合与字段值.md)：聚合统计、`Value`、`Column` 和复杂 `Count`。
- [分页与遍历](分页与遍历.md)：分页器、`Chunk` 和 `ChunkById`。

## 写入与事务

- [写入更新删除](写入更新删除.md)：单行插入、更新、删除和增减表达式。
- [批量写入](批量写入.md)：`InsertAll`、字段一致性、自动分批和原子性。
- [事务](事务.md)：闭包事务、手动事务、上下文、隔离级别和事务内查询。
- [数据库迁移](数据库迁移.md)：应用级迁移注册、校验和、批次、状态和回滚命令。
- [原生 SQL](原生SQL.md)：`Query`、`Execute`、`WhereRaw`、占位符和事务原生写入边界。

## 模型

- [模型](模型.md)：模型定义、表名推断、模型配置和应用层组织。
- [模型字段映射](模型字段映射.md)：结构体写入、`thinkgo` 标签、主键回填和零值策略。
- [模型生命周期](模型生命周期.md)：自动时间戳、软删除和模型事件。
- [模型查询](模型查询.md)：模型查询器、搜索器、获取器、修改器和终端方法。
- [模型关联](模型关联.md)：一对一、一对多、反向关联、多对多、立即查询和预加载。

## 错误与非 SQL 数据库

- [错误与安全](错误与安全.md)：标识符校验、全表写保护、错误判断和日志脱敏。
- [MongoDB](MongoDB.md)：MongoDB 连接、条件转换、结果规范化和写入限制。
- [Neo4j](Neo4j.md)：Neo4j Label、Cypher 条件、会话和写入限制。

旧的综合入口 [MongoDB 与 Neo4j](MongoDB与Neo4j.md) 仍保留，内容以本目录两个独立章节为准。
